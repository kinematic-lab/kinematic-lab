/**
 * Note that this is just and example,
 * and should be replaced.
 */
 
export default (options: LabModuleExampleOptions): LabModuleExample => {
	return {
		isExample: options.isExample ?? true,
	};
};
